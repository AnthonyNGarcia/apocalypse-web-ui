import React from 'react';
import './HoverDialog.css';

/**
 *
 * HoverDialog JSDocs
 *
 * @param {Object} props passed from the parent component
 * @return {JSX} to render
 */
const HoverDialog = (props) => {
  return (
    <React.Fragment>

    </React.Fragment>
  );
};

export default HoverDialog;
