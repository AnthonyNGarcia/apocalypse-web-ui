import React from 'react';
import './UnitPassive.css';

/**
 *
 * UnitPassive JSDocs
 *
 * @param {Object} props passed from the parent component
 * @return {JSX} to render
 */
const UnitPassive = (props) => {
  return (
    <React.Fragment>

    </React.Fragment>
  );
};

export default UnitPassive;
