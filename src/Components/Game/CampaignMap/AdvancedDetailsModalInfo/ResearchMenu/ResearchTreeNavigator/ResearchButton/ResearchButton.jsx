import React from 'react';
import './ResearchButton.css';

/**
 *
 * ResearchButton JSDocs
 *
 * @param {Object} props passed from the parent component
 * @return {JSX} to render
 */
const ResearchButton = (props) => {
  return (
    <React.Fragment>

    </React.Fragment>
  );
};

export default ResearchButton;
