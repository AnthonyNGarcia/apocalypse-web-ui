import React from 'react';
import './BuildMenuTab.css';

/**
 *
 * BuildMenuTab JSDocs
 *
 * @param {Object} props passed from the parent component
 * @return {JSX} to render
 */
const BuildMenuTab = (props) => {
  return (
    <React.Fragment>

    </React.Fragment>
  );
};

export default BuildMenuTab;
