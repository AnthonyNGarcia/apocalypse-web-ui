import React from 'react';
import './CurrentBuildingItem.css';

/**
 *
 * CurrentBuildingItem JSDocs
 *
 * @param {Object} props passed from the parent component
 * @return {JSX} to render
 */
const CurrentBuildingItem = (props) => {
  return (
    <React.Fragment>

    </React.Fragment>
  );
};

export default CurrentBuildingItem;
