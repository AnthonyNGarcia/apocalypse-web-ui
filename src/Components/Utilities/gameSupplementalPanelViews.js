/**
 * This file contains hard-coded main views for this application to distinguish
 * which view is being rendered in the Game Component. It is essentially to act
 * as an enum.
 */
const UNIT_INFO = 'UNIT_INFO';
const BUILDING_INFO = 'BUILDING_INFO';
const NONE = 'NONE';

const SUPPLEMENTAL_PANEL_VIEWS = {
  UNIT_INFO: UNIT_INFO,
  BUILDING_INFO: BUILDING_INFO,
  NONE: NONE,
};

export default SUPPLEMENTAL_PANEL_VIEWS;
