/**
 * This file contains hard-coded main views for this application to distinguish
 * which view is being rendered in the Lobby Component. It is essentially to act
 * as an enum.
 */
const BROWSE_LOBBIES_VIEW = 'BROWSE_LOBBIES_VIEW';
const IN_LOBBY_VIEW = 'IN_LOBBY_VIEW';

const LOBBY_VIEWS = {
  BROWSE_LOBBIES_VIEW: BROWSE_LOBBIES_VIEW,
  IN_LOBBY_VIEW: IN_LOBBY_VIEW,
};

export default LOBBY_VIEWS;
