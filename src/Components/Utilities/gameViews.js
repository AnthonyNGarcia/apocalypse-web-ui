/**
 * This file contains hard-coded main views for this application to distinguish
 * which view is being rendered in the Game Component. It is essentially to act
 * as an enum.
 */
const CAMPAIGN_MAP_VIEW = 'CAMPAIGN_MAP_VIEW';
const BATTLE_MAP_VIEW = 'BATTLE_MAP_VIEW';

const GAME_VIEWS = {
  CAMPAIGN_MAP_VIEW: CAMPAIGN_MAP_VIEW,
  BATTLE_MAP_VIEW: BATTLE_MAP_VIEW,
};

export default GAME_VIEWS;
